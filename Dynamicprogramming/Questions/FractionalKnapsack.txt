//its of greedy not dp

PriorityQueue<Pair> pq = new PriorityQueue<>((a, b) -> Double.compare(b.ratio, a.ratio));
		for (int i = 0; i< n ; i++){
            double r = (double) val[i]/ w[i];
			pq.add(new Pair(val[i], r, w[i]));
		}
		double ans = 0;
		while (!pq.isEmpty()){
			Pair p = pq.poll();
			if (max >= p.w){
				ans+=p.v;
				max-=p.w;
			}
            else{
                ans += (p.ratio * max);
                break;
            }
		}

		System.out.println(ans);
